import { Container, Text } from "@chakra-ui/react";

const Historie = () => {
  return (
    <Container centerContent maxW="container.md" p={4}>
      <Text fontSize="2xl" mb={4}>Historie</Text>
      <Text>Information about the family history and an option to update the family tree.</Text>
    </Container>
  );
};

export default Historie;